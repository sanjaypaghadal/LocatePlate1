@model Restaurant
@{Layout=null;}
@{ 
//var RatingGroups = Model?.ReviewRating?.GroupBy(c => c.Ratings.).Select(group =>
//                                                                 new RatingGroups
//                                                                 {
//                                                                     Key = group.Key.ToString(),
//                                                                     Value = group.ToList(),
//                                                                     Avg = group.Average(c => c.Rating),
//                                                                     AvgPercentage = ((group.Sum(c => c.Rating) / 5) * 100)
//                                                                 }).OrderBy(x => x.Key).ToList();




}
@foreach (var item in Model.ReviewRating)
{
 

    <div class="reviews-section border-radius-4">
        <div class="bg-white rounded p-4 mb-2 restaurant-detailed-ratings-and-reviews">
            <h5 class="mb-1 font-18">All Reviews</h5>
            <div class="reviews-members pt-4 pb-4">
                <div class="media">
                    <div class="media-body">
                        <div class="reviews-members-header">
                            <span class="star-rating float-right">
                                @await Html.PartialAsync("_Rating", item.Avg)
                            </span>
                            <h6 class="mb-1"><a class="text-black font-16" href="">@item.Reviews.CreatedBy</a></h6>
                            <p class="text-gray">@item.Reviews.CreatedDate.ToString("D")</p>
                        </div>
                        <div class="reviews-members-body">
                            <p>@item.Reviews.Review</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}

